# 关于该网站的说明：



## 网站功能说明：

这是一个手语换装小游戏网站。

「分社线上活动.docx」中保存了上装、下装、鞋子三种类别的各种服装以及对应的手语图片。

三个位置的不同类别的各种服装分别有一个阿拉伯数字进行标号。

这三个标号分别对应「奇迹小语」文件夹中的已经换装完成后的图片。

比如 毛衣-毛裤-高跟鞋 对应 322.png。开始游戏.png 是游戏还没开始的时候展示给大家的默认的换装图片。数字 0 代表当前这个身体位置还没有任何的服装。

你现在需要写一个网站，呈现这个小游戏。

玩家通过点击对应的手语图片，去给手小语这个角色进行换装。注意不要展示手语图片的释义，需要考察玩家对手语预判的理解能力。



## 我想在北京大学（PKU）校园网中部署这个网站，让校内的朋友可以访问：



Q: 你这台经常开着的电脑是什么系统？ A: Windows

Q: 你要部署的网站是什么类型？ A: 静态网页 (HTML/CSS/JS)

Q: 这台电脑现在的网络环境？ A: 连的是 PKU 无线



下面是暂定的方案：

## 1. 准备静态站文件

把你的 `index.html` 和相关资源放到一个文件夹里，比如 `D:\mysite\`。

## 2. 起一个 Web 服务器

**最快方案：Python 自带的 http.server**

如果还没装 Python，去 python.org 下个最新版，安装时勾选 "Add to PATH"。然后开 PowerShell：

```powershell
cd D:\mysite
python -m http.server 8080 --bind ::
```

`--bind ::` 让它同时监听 IPv4 和 IPv6（关键，默认只监听 IPv4）。这样起完，本机访问 `http://localhost:8080` 应该能看到页面。

**更稳的方案：Caddy（推荐长期跑）**

Python 那个关掉窗口就停了。Caddy 是单文件、零配置、还能自动续 HTTPS：

1. 去 caddyserver.com 下载 Windows 版（一个 `caddy.exe`）

2. 放到 `D:\mysite\` 里，在该目录开 PowerShell：

   ```powershell
   .\caddy.exe file-server --listen :8080 --browse
   ```

3. 想后台常驻可以装成 Windows 服务，Caddy 文档有说明

## 3. 找到你的 IPv6 地址

PowerShell 里：

```powershell
ipconfig
```

在"无线局域网适配器 WLAN"段，找 **IPv6 地址**那行，**只要 `2001:da8:` 开头的那个**（这是 CERNET2/北大公网 IPv6）。形如：

```
2001:da8:201:xxxx:xxxx:xxxx:xxxx:xxxx
```

⚠️ 不要用 `fe80::` 开头的（链路本地，出不了本机所在网段），也不要用 "临时 IPv6 地址"（会变）。如果看到多个 `2001:da8:` 地址，优先选**没有"临时"或"temporary"标签**的那个，这是相对稳定的地址。

## 4. 放行 Windows 防火墙

默认 Windows 会挡掉外部访问。开管理员 PowerShell，执行：

```powershell
New-NetFirewallRule -DisplayName "MySite 8080" -Direction Inbound -Protocol TCP -LocalPort 8080 -Action Allow
```

（之后想撤销：`Remove-NetFirewallRule -DisplayName "MySite 8080"`）



